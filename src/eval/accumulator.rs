#[path = "accumulator/assets.rs"]
mod assets;
#[path = "accumulator/camera.rs"]
mod camera;
#[path = "accumulator/content.rs"]
mod content;
#[path = "accumulator/initial_sample.rs"]
mod initial_sample;
#[path = "accumulator/movement.rs"]
mod movement;
#[path = "accumulator/objectives.rs"]
mod objectives;
#[path = "accumulator/observe.rs"]
mod observe;
#[path = "accumulator/summary_report.rs"]
mod summary_report;
#[path = "accumulator/world.rs"]
mod world;

use bevy::prelude::{Vec2, Vec3};

use super::sample::EvalSample;

#[derive(Default, Clone, Debug)]
pub struct EvalAccumulator {
    first_sample: Option<EvalSample>,
    final_sample: Option<EvalSample>,
    frame_times_ms: Vec<f32>,
    sample_count: u32,
    max_altitude_m: f32,
    min_altitude_m: f32,
    max_grounded_visual_foot_gap_m: f32,
    max_speed_mps: f32,
    max_camera_distance_m: f32,
    min_camera_surface_clearance_m: f32,
    max_camera_player_angle_degrees: f32,
    max_camera_step_distance_m: f32,
    max_camera_rotation_delta_degrees: f32,
    max_camera_orbit_alignment_degrees: f32,
    camera_follow_direction_error_sum_degrees: f32,
    camera_follow_direction_error_samples: u32,
    camera_follow_direction_error_values_degrees: Vec<f32>,
    max_camera_follow_direction_error_degrees: f32,
    max_abs_camera_view_yaw_degrees: f32,
    first_camera_view_yaw_degrees: Option<f32>,
    max_camera_view_yaw_drift_degrees: f32,
    first_camera_world_yaw_degrees: Option<f32>,
    max_camera_world_yaw_drift_degrees: f32,
    max_camera_obstruction_adjustment_m: f32,
    max_camera_obstruction_hits: usize,
    desired_body_heading_error_sum_degrees: f32,
    desired_body_heading_samples: u32,
    desired_body_heading_error_values_degrees: Vec<f32>,
    max_desired_body_heading_error_degrees: f32,
    previous_desired_body_yaw_error_degrees: Option<f32>,
    max_body_yaw_error_step_degrees: f32,
    previous_body_yaw_intent_axis: Option<Vec2>,
    previous_body_yaw_error_sign: Option<f32>,
    body_yaw_oscillation_count: u32,
    previous_body_roll_degrees: Option<f32>,
    max_body_roll_step_degrees: f32,
    max_right_body_bank_degrees: f32,
    max_left_body_bank_degrees: f32,
    lateral_body_travel_heading_error_values_degrees: Vec<f32>,
    right_lateral_body_travel_heading_samples: u32,
    left_lateral_body_travel_heading_samples: u32,
    max_lateral_body_travel_heading_error_degrees: f32,
    backward_diagonal_body_travel_heading_error_values_degrees: Vec<f32>,
    backward_right_diagonal_body_travel_heading_samples: u32,
    backward_left_diagonal_body_travel_heading_samples: u32,
    max_backward_diagonal_body_travel_heading_error_degrees: f32,
    desired_travel_heading_error_values_degrees: Vec<f32>,
    right_desired_travel_heading_samples: u32,
    left_desired_travel_heading_samples: u32,
    backward_right_desired_travel_heading_samples: u32,
    backward_left_desired_travel_heading_samples: u32,
    max_desired_travel_heading_error_degrees: f32,
    pure_air_turn_sideways_body_travel_heading_error_values_degrees: Vec<f32>,
    max_pure_air_turn_sideways_body_travel_heading_error_degrees: f32,
    pure_air_turn_sideways_desired_travel_heading_error_values_degrees: Vec<f32>,
    max_pure_air_turn_sideways_desired_travel_heading_error_degrees: f32,
    right_pure_air_turn_sideways_samples: u32,
    left_pure_air_turn_sideways_samples: u32,
    pure_air_turn_sideways_segment_axis: Option<f32>,
    pure_air_turn_sideways_segment_start_time_secs: Option<f32>,
    max_desired_heading_alignment_mps: f32,
    max_lateral_response_mps: f32,
    first_lateral_input_time_secs: Option<f32>,
    first_lateral_response_time_secs: Option<f32>,
    max_right_lateral_response_mps: f32,
    first_right_lateral_input_time_secs: Option<f32>,
    first_right_lateral_response_time_secs: Option<f32>,
    max_left_lateral_response_mps: f32,
    first_left_lateral_input_time_secs: Option<f32>,
    first_left_lateral_response_time_secs: Option<f32>,
    max_backward_lateral_response_mps: f32,
    first_backward_lateral_input_time_secs: Option<f32>,
    first_backward_lateral_response_time_secs: Option<f32>,
    max_backward_right_lateral_response_mps: f32,
    max_backward_right_rear_response_mps: f32,
    first_backward_right_lateral_input_time_secs: Option<f32>,
    first_backward_right_lateral_response_time_secs: Option<f32>,
    max_backward_left_lateral_response_mps: f32,
    max_backward_left_rear_response_mps: f32,
    first_backward_left_lateral_input_time_secs: Option<f32>,
    first_backward_left_lateral_response_time_secs: Option<f32>,
    backward_air_control_start_speed_mps: Option<f32>,
    min_backward_air_control_speed_mps: Option<f32>,
    backward_air_control_start_planar_speed_mps: Option<f32>,
    min_backward_air_control_planar_speed_mps: Option<f32>,
    max_air_brake_speed_drop_mps: f32,
    max_air_brake_planar_speed_drop_mps: f32,
    max_post_brake_forward_alignment_mps: f32,
    max_pose_torso_pitch_degrees: f32,
    max_pose_arm_spread_degrees: f32,
    max_pose_leg_tuck_degrees: f32,
    max_dive_pose_torso_pitch_degrees: f32,
    max_dive_pose_arm_spread_degrees: f32,
    max_dive_pose_leg_tuck_degrees: f32,
    max_pose_lateral_lean_degrees: f32,
    max_right_pose_lateral_lean_degrees: f32,
    max_left_pose_lateral_lean_degrees: f32,
    max_backward_right_air_brake_pose_lateral_lean_degrees: f32,
    max_backward_left_air_brake_pose_lateral_lean_degrees: f32,
    max_grounded_walk_stride_foot_travel_m: f32,
    max_grounded_run_stride_foot_travel_m: f32,
    max_grounded_walk_stride_leg_opposition_degrees: f32,
    max_grounded_run_stride_leg_opposition_degrees: f32,
    max_pose_landing_crouch_m: f32,
    max_pose_landing_foot_forward_m: f32,
    max_pose_landing_foot_split_m: f32,
    max_pose_landing_flare_degrees: f32,
    max_pose_landing_recovery_flip_degrees: f32,
    max_pose_wing_airflow_strength: f32,
    max_pose_scarf_stream_m: f32,
    max_pose_scarf_lateral_sway_m: f32,
    max_pose_scarf_tail_flex_degrees: f32,
    min_key_pose_readability_score: Option<f32>,
    max_key_pose_readability_score: f32,
    unreadable_key_pose_samples: u32,
    pose_grounded_idle_samples: u32,
    pose_grounded_walk_samples: u32,
    pose_grounded_run_samples: u32,
    pose_launching_samples: u32,
    pose_falling_samples: u32,
    key_pose_transition_grace_samples: u32,
    max_visible_pose_part_count: u32,
    pose_temporal_stability_samples: u32,
    max_pose_part_rotation_delta_degrees: f32,
    max_pose_part_translation_delta_m: f32,
    min_pose_limb_clearance_m: Option<f32>,
    landing_pose_temporal_stability_samples: u32,
    max_landing_pose_part_rotation_delta_degrees: f32,
    max_landing_pose_part_translation_delta_m: f32,
    max_authored_glider_response_degrees: f32,
    max_authored_glider_motion_m: f32,
    gliding_dive_samples: u32,
    max_authored_glider_dive_response_degrees: f32,
    max_authored_glider_dive_motion_m: f32,
    authored_clip_match_samples: u32,
    authored_clip_mismatch_samples: u32,
    authored_bank_left_clip_samples: u32,
    authored_bank_right_clip_samples: u32,
    authored_grounded_idle_clip_samples: u32,
    authored_grounded_walk_clip_samples: u32,
    authored_grounded_run_clip_samples: u32,
    authored_launch_clip_samples: u32,
    authored_glide_clip_samples: u32,
    authored_fall_clip_samples: u32,
    authored_dive_clip_samples: u32,
    authored_air_brake_clip_samples: u32,
    authored_land_clip_samples: u32,
    authored_transition_active_samples: u32,
    max_authored_transition_elapsed_ms: u64,
    max_authored_transition_duration_ms: u64,
    max_authored_transition_progress: f32,
    min_target_distance_m: f32,
    min_camera_pitch_degrees: f32,
    max_camera_pitch_degrees: f32,
    max_abs_camera_yaw_offset_degrees: f32,
    min_camera_pitch_offset_degrees: f32,
    max_camera_pitch_offset_degrees: f32,
    max_visible_wind_fields: usize,
    max_dynamic_wind_flow_fields: usize,
    max_wind_flow_speed_mps: f32,
    max_wind_flow_variation: f32,
    max_wind_flow_direction_change_degrees: f32,
    min_dynamic_readable_wind_flow_variation: Option<f32>,
    max_dynamic_readable_wind_flow_variation: f32,
    max_wind_flow_variation_range: f32,
    wind_force_samples: u32,
    meaningful_wind_force_samples: u32,
    aligned_wind_force_samples: u32,
    crosswind_force_samples: u32,
    aligned_crosswind_force_samples: u32,
    updraft_swirl_force_samples: u32,
    aligned_updraft_swirl_force_samples: u32,
    layered_wind_force_samples: u32,
    aligned_layered_wind_force_samples: u32,
    crosswind_updraft_overlap_samples: u32,
    aligned_crosswind_updraft_overlap_samples: u32,
    max_active_wind_force_fields: usize,
    max_crosswind_force_fields: usize,
    max_updraft_swirl_force_fields: usize,
    max_layered_wind_force_fields: usize,
    max_wind_force_delta_mps: f32,
    max_crosswind_force_delta_mps: f32,
    max_updraft_swirl_force_delta_mps: f32,
    max_layered_wind_force_delta_mps: f32,
    max_wind_force_flow_speed_mps: f32,
    max_wind_force_variation: f32,
    max_wind_force_flow_alignment: f32,
    max_crosswind_force_flow_alignment: f32,
    max_updraft_swirl_force_flow_alignment: f32,
    max_layered_wind_force_flow_alignment: f32,
    max_wind_force_aligned_delta_mps: f32,
    max_crosswind_force_aligned_delta_mps: f32,
    max_updraft_swirl_force_aligned_delta_mps: f32,
    max_layered_wind_force_aligned_delta_mps: f32,
    wind_load_response_samples: u32,
    max_wind_load_lateral_load: f32,
    max_wind_load_pose_lean_degrees: f32,
    max_wind_load_glider_response_degrees: f32,
    crosswind_neutral_previous_position: Option<Vec3>,
    crosswind_neutral_drift_samples: u32,
    crosswind_neutral_horizontal_drift_m: f32,
    max_crosswind_neutral_horizontal_step_m: f32,
    max_active_lift_fields: usize,
    max_readable_lift_fields: usize,
    max_sky_island_count: usize,
    max_active_chunk_count: usize,
    max_active_island_count: usize,
    max_near_lod_islands: usize,
    max_mid_lod_islands: usize,
    max_far_lod_islands: usize,
    max_visible_island_terrain_count: usize,
    max_hidden_island_terrain_count: usize,
    max_visible_island_impostor_count: usize,
    max_hidden_island_impostor_count: usize,
    max_visible_island_detail_count: usize,
    max_hidden_island_detail_count: usize,
    max_visible_route_beacon_count: usize,
    max_weather_cloud_count: usize,
    max_environment_motion_visual_count: usize,
    max_environment_motion_offset_m: f32,
    max_updraft_guide_visual_count: usize,
    max_updraft_ribbon_visual_count: usize,
    max_crosswind_guide_visual_count: usize,
    max_crosswind_ribbon_visual_count: usize,
    max_updraft_visual_motion_m: f32,
    max_updraft_visual_rise_m: f32,
    max_updraft_visual_swirl_displacement_m: f32,
    max_updraft_visual_depth_span_m: f32,
    max_updraft_visual_scale_pulse: f32,
    max_crosswind_visual_motion_m: f32,
    max_crosswind_guide_flow_displacement_m: f32,
    max_crosswind_ribbon_flow_displacement_m: f32,
    max_crosswind_visual_lane_depth_span_m: f32,
    max_crosswind_visual_scale_pulse: f32,
    max_updraft_flow_coherent_visual_count: usize,
    max_crosswind_flow_coherent_visual_count: usize,
    max_crosswind_ribbon_flow_coherent_sample_count: usize,
    max_updraft_visual_flow_alignment: f32,
    max_crosswind_visual_flow_alignment: f32,
    max_crosswind_ribbon_visual_flow_alignment: f32,
    max_observed_updraft_flow_coherent_visual_count: usize,
    max_observed_crosswind_flow_coherent_visual_count: usize,
    max_observed_crosswind_ribbon_flow_coherent_sample_count: usize,
    max_observed_updraft_visual_frame_motion_m: f32,
    max_observed_updraft_visual_frame_rise_m: f32,
    max_observed_updraft_visual_frame_swirl_displacement_m: f32,
    max_observed_crosswind_visual_frame_motion_m: f32,
    max_observed_crosswind_guide_frame_flow_displacement_m: f32,
    max_observed_crosswind_ribbon_frame_flow_displacement_m: f32,
    max_observed_updraft_visual_speed_mps: f32,
    max_observed_crosswind_visual_speed_mps: f32,
    max_observed_wind_visual_acceleration_mps2: f32,
    observed_wind_visual_jump_count: u32,
    max_observed_updraft_visual_flow_alignment: f32,
    max_observed_crosswind_visual_flow_alignment: f32,
    max_observed_crosswind_ribbon_visual_flow_alignment: f32,
    max_updraft_field_count: usize,
    max_updraft_fields_with_guides_count: usize,
    max_updraft_fields_with_ribbons_count: usize,
    max_updraft_fields_with_guides_and_ribbons_count: usize,
    max_updraft_flow_coherent_field_count: usize,
    max_crosswind_field_count: usize,
    max_crosswind_fields_with_guides_count: usize,
    max_crosswind_fields_with_ribbons_count: usize,
    max_crosswind_fields_with_guides_and_ribbons_count: usize,
    max_crosswind_flow_coherent_field_count: usize,
    sustained_wind_visual_flow_samples: u32,
    sustained_updraft_visual_flow_samples: u32,
    sustained_crosswind_visual_flow_samples: u32,
    sustained_crosswind_ribbon_advected_flow_samples: u32,
    max_world_collision_proxy_count: usize,
    max_terrain_rim_collision_proxy_count: usize,
    max_terrain_body_collision_proxy_count: usize,
    max_solid_world_collision_proxy_count: usize,
    max_tree_world_collision_proxy_count: usize,
    max_rock_world_collision_proxy_count: usize,
    max_landmark_world_collision_proxy_count: usize,
    world_collision_resolved_samples: u32,
    world_collision_contact_samples: u32,
    terrain_rim_collision_resolved_samples: u32,
    terrain_rim_collision_contact_samples: u32,
    terrain_body_collision_resolved_samples: u32,
    terrain_body_collision_contact_samples: u32,
    max_world_collision_push_m: f32,
    max_terrain_rim_collision_push_m: f32,
    max_terrain_body_collision_push_m: f32,
    min_island_terrain_surface_count: usize,
    min_island_terrain_mesh_vertices: usize,
    min_island_terrain_color_bands: usize,
    min_island_terrain_material_weight_bands: usize,
    min_island_terrain_material_channels: usize,
    min_island_terrain_material_regions: usize,
    min_island_terrain_texture_detail_bands: usize,
    min_island_terrain_relief_range_m: f32,
    min_island_terrain_archetype_count: usize,
    min_island_cliff_color_bands: usize,
    min_island_impostor_mesh_vertices: usize,
    min_island_impostor_color_bands: usize,
    min_procedural_island_body_count: usize,
    max_primitive_island_body_count: usize,
    min_island_body_silhouette_segments: usize,
    max_avg_island_body_silhouette_segments: f32,
    min_island_body_mesh_vertices: usize,
    max_island_body_mesh_vertices: usize,
    min_generated_ground_cover_patch_count: usize,
    min_ground_cover_blade_count: usize,
    min_ground_cover_mesh_vertices: usize,
    min_generated_tree_trunk_count: usize,
    min_generated_tree_canopy_count: usize,
    min_tree_trunk_mesh_vertices: usize,
    min_tree_canopy_mesh_vertices: usize,
    min_detail_biome_palette_count: usize,
    min_generated_rock_count: usize,
    min_rock_mesh_vertices: usize,
    min_generated_landmark_count: usize,
    min_generated_route_cairn_count: usize,
    min_generated_launch_beacon_count: usize,
    min_generated_landing_garden_marker_count: usize,
    min_generated_pond_surface_count: usize,
    min_landmark_mesh_vertices: usize,
    min_generated_weather_cloud_count: usize,
    min_generated_weather_cloud_bank_count: usize,
    min_weather_cloud_bank_depth_m: f32,
    min_weather_cloud_lobe_count: usize,
    min_max_weather_cloud_lobe_count: usize,
    min_weather_cloud_mesh_vertices: usize,
    min_weather_cloud_filament_ribbon_detail_count: usize,
    max_resident_island_visual_count: usize,
    max_stream_visibility_changes_per_frame: usize,
    total_stream_visibility_changes: usize,
    max_catalog_island_visual_count: usize,
    max_hidden_island_visual_count: usize,
    max_resident_island_visual_fraction: f32,
    max_stream_spawned_visuals_per_frame: usize,
    max_stream_despawned_visuals_per_frame: usize,
    total_stream_spawned_visuals: usize,
    total_stream_despawned_visuals: usize,
    max_entity_count: usize,
    max_objective_total_count: usize,
    max_completed_objective_count: usize,
    min_objective_distance_m: f32,
    objective_complete_samples: u32,
    max_visual_asset_slot_count: usize,
    max_gltf_scene_asset_slot_count: usize,
    max_ready_visual_asset_slot_count: usize,
    max_placeholder_visual_asset_slot_count: usize,
    max_streaming_visual_asset_slot_count: usize,
    max_missing_visual_asset_slot_count: usize,
    max_deferred_visual_asset_scene_count: usize,
    max_queued_visual_asset_scene_count: usize,
    max_loading_visual_asset_scene_count: usize,
    max_loaded_visual_asset_scene_count: usize,
    max_dependency_loaded_visual_asset_scene_count: usize,
    max_preload_ready_visual_asset_scene_count: usize,
    max_failed_visual_asset_scene_count: usize,
    max_spawned_visual_asset_scene_count: usize,
    max_ready_visual_asset_scene_count: usize,
    max_visible_authored_world_fixture_count: usize,
    max_always_visual_asset_slot_count: usize,
    max_stream_window_visual_asset_slot_count: usize,
    max_near_lod_visual_asset_slot_count: usize,
    max_far_lod_visual_asset_slot_count: usize,
    max_weather_visual_asset_slot_count: usize,
    max_always_preload_ready_visual_asset_slot_count: usize,
    max_streaming_preload_ready_visual_asset_slot_count: usize,
    max_declared_animation_clip_count: usize,
    max_ready_animation_clip_count: usize,
    max_animation_player_count: usize,
    max_animation_graph_count: usize,
    max_power_up_count: usize,
    min_visible_power_up_count: usize,
    max_collected_power_up_count: usize,
    power_up_effect_samples: u32,
    total_power_up_activations: usize,
    target_landing_samples: u32,
    lifted_samples: u32,
    readable_lift_samples: u32,
    unreadable_lift_samples: u32,
    dynamic_readable_lift_samples: u32,
    pose_gliding_samples: u32,
    pose_air_turn_samples: u32,
    right_pose_air_turn_samples: u32,
    left_pose_air_turn_samples: u32,
    pose_diving_samples: u32,
    pose_air_brake_samples: u32,
    right_pose_air_brake_samples: u32,
    left_pose_air_brake_samples: u32,
    backward_right_pose_air_brake_samples: u32,
    backward_left_pose_air_brake_samples: u32,
    pose_landing_anticipation_samples: u32,
    pose_landing_recovery_samples: u32,
    gliding_samples: u32,
    launching_samples: u32,
    grounded_samples: u32,
}
